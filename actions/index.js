const { open2tab } = require("./open-to-tab");
const { generateId } = require("./generate-id");
const { apply } = require("./apply");
const { duplicate } = require("./duplicate");

module.exports = {
  open2tab,
  generateId,
  apply,
  duplicate,
};
